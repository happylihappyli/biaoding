using Common_Robot2;
using ConverxHull;

namespace Test1
{

    //保存全局变量
    public class S_Read_Global : C_Node
    {
        public string path = "";
        public string encode = "";

        public delegate void AppendConsoleText(string text);
        public AppendConsoleText? callback_cmd;
        public E_Cmd_Event pEvent = null;

        public S_Read_Global(string name, C_Space space_parent, C_Space space) :
            base(name, space_parent, space)
        {
        }

        public override void init()
        {

        }
        public override Task run_sub()
        {
            run_sub_main();
            return Task.CompletedTask;
        }

        private void run_sub_main()
        {
        }

        private void textBox1_AppendConsoleText(string text)
        {
            if (pEvent == null)
            {
                Main.WriteLine(this, "pEvent == null");
                return;
            }
            pEvent.msg2 = text;// .run_sub2(text);
            pEvent.Run(pTrain);
        }

    }

    
}
