using Common_Robot;
using Common_Robot2;
using ConverxHull;

namespace Test1
{
    public class E_TCP_CallBack : C_Node
    {

        public string key_control = "";

        public string msg = "";
        public string hex = "";
        public string str_new = "";


        public E_TCP_CallBack(string name, C_Space space_parent, C_Space space) :
            base(name,space_parent, space)
        {
            //Name = name;
            //space.vars_step.Add(name, this);
        }

        public override Task run_sub()
        {
            Main.WriteLine(this, Name + " 事件激活 " + this.GetType().Name);

            //Random rnd = new Random();
            //string id = "TCP_" + rnd.Next(100);
            //pTrain = new C_Train(id);

            run_sub_main();

            return Task.CompletedTask;
        }

        private void run_sub_main()
        { 

        }


        public override void init()
        {
            I_TCP pServer = (I_TCP)space.vars_step[key_control];
            pServer.pCallBack.Add(new C_CallBack_Item(Name, msg, hex));
        }
    }


}
