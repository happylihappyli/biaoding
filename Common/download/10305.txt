
using Common_Robot2;

namespace Test1
{
    public class E_Form_Unload : C_Node
    {

        public E_Form_Unload(string name, C_Space space_parent, C_Space space) :
            base(name,space_parent, space)
        {
        }

        public override void init()
        {

        }

        public override Task run_sub()
        {
            Main.WriteLine(this, " 事件激活 " + this.GetType().Name);
            Random rnd = new Random();
            string id = "Unload_" + rnd.Next(100);
            pTrain = new C_Train(id);
            run_sub_main();
            return Task.CompletedTask;
        }

        public void run_sub_main()
        {

        }
    }
}
