
using Common_Robot2;
using ConverxHull;

namespace Test1
{
    public class C_Cloud_Fit_Planet : C_Node
    {
        public string key_cloud = "cloud1";
        public string key_camera = "";
        public string key_save = "m1";
        public string key_point_mid = "m1";

        public C_Cloud_Fit_Planet(string name, C_Space space_parent, C_Space space) : base(name,space_parent, space)
        {
        }

        public override void init()
        {
        }

        public override Task run_sub()
        {
            run_sub_main();
            return Task.CompletedTask;
        }

        public void run_sub_main()
        {
            C_Camera_TuYang camera1 = (C_Camera_TuYang)this.read_var(this.key_camera, "C_Camera_TuYang");
            List<C_Point3D> pListPoint = (List<C_Point3D>)this.read_var(this.key_cloud, "List<C_Point3D>");

            C_Point3D faxiangliang2 = Main.平面拟合计算法向量等(pListPoint);
            (C_Point3D mid_center, _, _) = Main.计算中位数中心点坐标(pListPoint);

            this.save_var(this.key_save, "C_Point3D", faxiangliang2);
            this.save_var(this.key_point_mid, "C_Point3D", mid_center);
        }
    }
}
